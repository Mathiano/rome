# Style anchor

`anchor-v1.jpg` is the reference every building render is prompted against; `PROMPTS.md` holds the prompts. Both are supplied by the Owner and were not yet in the repo when the pipeline was written. Run `python3 tools/artgen/build.py assets/style/anchor-v1.jpg --out assets/style` to check the anchor itself passes the plate test.
